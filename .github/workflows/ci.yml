name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  validate:
    runs-on: [self-hosted, linux, arm64, fast]
    strategy:
      matrix:
        node-version: [18, 20]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - run: npm ci

      - name: Run tests
        run: npm test
        env:
          DAVOXI_TEST_API_KEY: ${{ secrets.DAVOXI_TEST_API_KEY }}

      - name: Validate app structure
        run: node -e "const app = require('./index'); console.log('OK:', Object.keys(app.triggers).length, 'triggers,', Object.keys(app.creates).length, 'creates,', Object.keys(app.searches).length, 'searches')"
